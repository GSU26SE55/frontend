// Shared KB Article schema — real source lives in shared (100% identical across admin/manager/staff).
export {
  kbArticleSchema,
  type KbArticleFormInput,
  type KbArticleFormValues,
} from "@/shared/schemas/kb/kb-article.schema";
