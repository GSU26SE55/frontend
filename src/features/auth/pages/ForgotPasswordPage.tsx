import { Card, CardContent, CardHeader, CardTitle } from '@/shared/components/ui/card'

export function ForgotPasswordPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Quên mật khẩu</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">Trang quên mật khẩu — sẽ implement ở ticket tiếp theo.</p>
      </CardContent>
    </Card>
  )
}
