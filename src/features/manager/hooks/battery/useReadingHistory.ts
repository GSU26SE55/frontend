// Wrapper that keeps the old name — the real source is shared/hooks/battery/useReadingHistory.ts.
export { useReadingHistoryLatest as useManagerReadingHistory } from "@/shared/hooks/battery/useReadingHistory";
