// Wrapper that keeps the old name — the real source lives in shared/hooks/battery/useBatteryAsset.ts.
export { useBatteryAsset as useStaffBatteryAsset } from "@/shared/hooks/battery/useBatteryAsset";
