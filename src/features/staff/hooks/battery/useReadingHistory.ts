// Wrapper that keeps the old name — the real source lives in shared/hooks/battery/useReadingHistory.ts.
export { useReadingHistoryLatest as useStaffReadingHistory } from "@/shared/hooks/battery/useReadingHistory";
