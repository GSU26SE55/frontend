// Shared KB Article schema — the real source lives in shared (identical for admin/manager/staff).
export {
  kbArticleSchema,
  type KbArticleFormInput,
  type KbArticleFormValues,
} from "@/shared/schemas/kb/kb-article.schema";
